{
	"name": "{{textdomain}}",
	"version": "{{version}}",
	"private": true,
	"description": "{{description}}",
	"license": "GPL-2.0-or-later",
	"scripts": {
		"build": "wp-scripts build",
		"start": "wp-scripts start"
	},
	"devDependencies": {
		"@wordpress/scripts": "{{wp_scripts_version}}"
	}
}
